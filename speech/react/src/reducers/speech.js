import {

} from '../constants';

const initialState = {
  showSpaceIndicator: false,
  // uploads: {}
};

export default function speech(state = initialState, action) {
  switch (action.type) {

    default:
      return state;
  }
}
